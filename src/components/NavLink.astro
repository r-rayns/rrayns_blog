---
export interface Props {
  href: string;
}

const { href } = Astro.props;

const active: boolean = href === Astro.url.pathname;
---

<a href={href} class:list={["dark:text-cyan-400 text-cyan-800", { "text-decoration-line: underline": !active }]}><slot /></a>
