---
export interface Props {
  src: string;
}

const { src } = Astro.props as Props;
---

<video width="100%" muted="true" controls>
  <source src={src} type="video/mp4" />
  Your browser does not support HTML5 video.
</video>
